# cloud-automation-utils
# Cloud Automation Utilities

Collection of Python scripts for automated provisioning and management of cloud resources across AWS and Azure.

---

## 🛠 Tech Stack
- Python
- Terraform
- AWS CLI / Azure CLI

---

## 🚀 Features
- Automatically spin up cloud infrastructure
- Manage virtual machines, storage, and networking
- Cross-cloud compatibility (AWS & Azure)

---

## 📋 Setup Instructions
1. Clone the repo: `git clone https://github.com/vyshnavidanda336-ctrl/cloud-automation-utils.git`
2. Install dependencies: `pip install -r requirements.txt`
3. Configure cloud credentials
4. Run scripts: `python create_vm.py` or `python provision_storage.py`
